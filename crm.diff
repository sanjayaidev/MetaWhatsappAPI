diff --git a/migrations/002_oauth_flows_sheet_watchers.sql b/migrations/002_oauth_flows_sheet_watchers.sql
new file mode 100644
index 0000000..4a9c94f
--- /dev/null
+++ b/migrations/002_oauth_flows_sheet_watchers.sql
@@ -0,0 +1,120 @@
+-- ═══════════════════════════════════════════════════════════════
+-- WaBlast Pro CRM — Migration 002
+-- Adds tables referenced by src/routes/flows.js, src/routes/sheet-watchers.js,
+-- src/sheet-poller.js, and src/google-auth.js that were never created in
+-- 001_complete_crm_schema.sql. Run this AFTER 001.
+-- ═══════════════════════════════════════════════════════════════
+
+-- ── OAuth tokens (Google Sheets/Drive/Gmail, Facebook, Instagram) ─────
+-- Used by src/google-auth.js (getValidGoogleAccessToken) and
+-- src/routes/flows.js (oauth-url / callback / status / disconnect).
+CREATE TABLE IF NOT EXISTS wb_oauth_tokens (
+  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
+  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
+  service TEXT NOT NULL CHECK (service IN ('google', 'facebook', 'instagram')),
+  token_type TEXT DEFAULT 'oauth2',
+  state TEXT UNIQUE,                 -- transient CSRF/lookup value while the OAuth dance is in flight
+  access_token_enc TEXT,
+  refresh_token_enc TEXT,
+  expires_at TIMESTAMPTZ,
+  scopes JSONB DEFAULT '[]',
+  metadata JSONB DEFAULT '{}',       -- e.g. { email, pages: [...] }
+  created_at TIMESTAMPTZ DEFAULT NOW(),
+  updated_at TIMESTAMPTZ DEFAULT NOW(),
+  UNIQUE(user_id, service)
+);
+CREATE INDEX IF NOT EXISTS idx_wb_oauth_tokens_user ON wb_oauth_tokens(user_id, service);
+
+DROP TRIGGER IF EXISTS trg_wb_oauth_tokens_updated_at ON wb_oauth_tokens;
+CREATE TRIGGER trg_wb_oauth_tokens_updated_at BEFORE UPDATE ON wb_oauth_tokens
+  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
+
+-- ── Sheet watchers (polling-based sheet automation) ───────────────────
+-- Used by src/routes/sheet-watchers.js (CRUD) and src/sheet-poller.js (the tick).
+CREATE TABLE IF NOT EXISTS wb_sheet_watchers (
+  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
+  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
+  spreadsheet_id TEXT NOT NULL,
+  spreadsheet_name TEXT,
+  worksheet TEXT NOT NULL,
+  watch_type TEXT NOT NULL CHECK (watch_type IN ('new_row', 'date_reminder')),
+  name_column TEXT,
+  phone_column TEXT,
+  email_column TEXT,
+  date_column TEXT,
+  offset_days INT DEFAULT 0,
+  message_template TEXT,
+  channel TEXT NOT NULL DEFAULT 'whatsapp' CHECK (channel IN ('whatsapp', 'email', 'instagram', 'facebook')),
+  template_id UUID REFERENCES wb_interactive_templates(id) ON DELETE SET NULL,
+  placeholder_mapping JSONB DEFAULT '{}',
+  poll_interval_minutes INT NOT NULL DEFAULT 15,
+  active BOOLEAN DEFAULT true,
+  last_row_count INT DEFAULT 0,
+  last_polled_at TIMESTAMPTZ,
+  last_error TEXT,
+  fired_log JSONB DEFAULT '{}',      -- date_reminder dedupe: { "<rowIndex>": "YYYY-M-D" }
+  created_at TIMESTAMPTZ DEFAULT NOW(),
+  updated_at TIMESTAMPTZ DEFAULT NOW()
+);
+CREATE INDEX IF NOT EXISTS idx_wb_sheet_watchers_user ON wb_sheet_watchers(user_id);
+CREATE INDEX IF NOT EXISTS idx_wb_sheet_watchers_active ON wb_sheet_watchers(active) WHERE active = true;
+
+DROP TRIGGER IF EXISTS trg_wb_sheet_watchers_updated_at ON wb_sheet_watchers;
+CREATE TRIGGER trg_wb_sheet_watchers_updated_at BEFORE UPDATE ON wb_sheet_watchers
+  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
+
+-- ── Visual flow builder ────────────────────────────────────────────────
+-- Used by src/routes/flows.js.
+CREATE TABLE IF NOT EXISTS wb_flows (
+  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
+  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
+  name TEXT NOT NULL DEFAULT 'Untitled Flow',
+  description TEXT DEFAULT '',
+  status TEXT DEFAULT 'draft' CHECK (status IN ('draft', 'active', 'paused')),
+  trigger_config JSONB DEFAULT '{}',
+  nodes JSONB DEFAULT '[]',
+  edges JSONB DEFAULT '[]',
+  variables JSONB DEFAULT '{}',
+  last_run_at TIMESTAMPTZ,
+  created_at TIMESTAMPTZ DEFAULT NOW(),
+  updated_at TIMESTAMPTZ DEFAULT NOW()
+);
+CREATE INDEX IF NOT EXISTS idx_wb_flows_user ON wb_flows(user_id);
+
+DROP TRIGGER IF EXISTS trg_wb_flows_updated_at ON wb_flows;
+CREATE TRIGGER trg_wb_flows_updated_at BEFORE UPDATE ON wb_flows
+  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();
+
+CREATE TABLE IF NOT EXISTS wb_flow_executions (
+  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
+  flow_id UUID NOT NULL REFERENCES wb_flows(id) ON DELETE CASCADE,
+  user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
+  status TEXT DEFAULT 'running' CHECK (status IN ('running', 'completed', 'failed')),
+  trigger_data JSONB DEFAULT '{}',
+  result JSONB DEFAULT '{}',
+  error TEXT,
+  started_at TIMESTAMPTZ DEFAULT NOW(),
+  completed_at TIMESTAMPTZ
+);
+CREATE INDEX IF NOT EXISTS idx_wb_flow_executions_flow ON wb_flow_executions(flow_id, started_at DESC);
+
+-- ── RLS ─────────────────────────────────────────────────────────────
+-- (server.js talks to Supabase via the service_role key, which bypasses
+-- RLS — these policies matter only if these tables are ever queried with
+-- an anon/user-scoped key, e.g. directly from the browser.)
+ALTER TABLE wb_oauth_tokens ENABLE ROW LEVEL SECURITY;
+ALTER TABLE wb_sheet_watchers ENABLE ROW LEVEL SECURITY;
+ALTER TABLE wb_flows ENABLE ROW LEVEL SECURITY;
+ALTER TABLE wb_flow_executions ENABLE ROW LEVEL SECURITY;
+
+DROP POLICY IF EXISTS "Users can manage own oauth tokens" ON wb_oauth_tokens;
+CREATE POLICY "Users can manage own oauth tokens" ON wb_oauth_tokens FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
+
+DROP POLICY IF EXISTS "Users can manage own sheet watchers" ON wb_sheet_watchers;
+CREATE POLICY "Users can manage own sheet watchers" ON wb_sheet_watchers FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
+
+DROP POLICY IF EXISTS "Users can manage own flows" ON wb_flows;
+CREATE POLICY "Users can manage own flows" ON wb_flows FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
+
+DROP POLICY IF EXISTS "Users can manage own flow executions" ON wb_flow_executions;
+CREATE POLICY "Users can manage own flow executions" ON wb_flow_executions FOR ALL USING (auth.uid() = user_id) WITH CHECK (auth.uid() = user_id);
diff --git a/public/crm.html b/public/crm.html
index 0fc3173..ec4fdf6 100644
--- a/public/crm.html
+++ b/public/crm.html
@@ -137,6 +137,7 @@ input:focus, select:focus, textarea:focus { outline: none; border-color: var(--g
 <div class="nav-item" data-tab="sources"><span class="nav-icon">🔌</span> Sources</div>
 <div class="nav-item" data-tab="fieldmapping"><span class="nav-icon">🗂️</span> Field Mapping</div>
 <div class="nav-item" data-tab="automations"><span class="nav-icon">⚡</span> Automations</div>
+<div class="nav-item" data-tab="sheetwatchers"><span class="nav-icon">🔔</span> Sheet Reminders</div>
 <div class="nav-item" data-tab="chatbot"><span class="nav-icon">🤖</span> AI Chatbot</div>
 </div>
 <div class="nav-section">
@@ -324,6 +325,14 @@ input:focus, select:focus, textarea:focus { outline: none; border-color: var(--g
 </div>
 </div>
 
+<!-- ══ SHEET REMINDERS (polling: new-row auto-send + date-based wishes) ══ -->
+<div class="tab-panel" id="tab-sheetwatchers">
+<div class="page-header"><div><div class="page-title">Sheet Reminders</div><div class="page-sub">Auto-send approved WhatsApp templates from a linked Google Sheet — new rows, or recurring dates like birthdays and fee due-dates</div></div><button class="btn btn-primary" onclick="openWatcherModal()">+ New Watcher</button></div>
+<div class="card">
+<div id="watchersList"><div class="empty-state"><div class="empty-icon">🔔</div><p>No sheet watchers yet. Connect a Google Sheet under Sources first, then add one here.</p></div></div>
+</div>
+</div>
+
 <!-- ══ AI CHATBOT ══ -->
 <div class="tab-panel" id="tab-chatbot">
 <div class="page-header"><div><div class="page-title">AI Chatbot</div><div class="page-sub">Website widget + your in-dashboard assistant</div></div></div>
@@ -496,6 +505,68 @@ input:focus, select:focus, textarea:focus { outline: none; border-color: var(--g
 </div>
 </div>
 
+<!-- ══ SHEET WATCHER MODAL ══ -->
+<div class="modal" id="watcherModal">
+<div class="modal-box wide">
+<div class="modal-header"><div class="modal-title" id="watcherModalTitle">New Sheet Watcher</div><button class="modal-close" onclick="closeModal('watcherModal')">✕</button></div>
+
+<div class="form-label">Google Sheet</div>
+<div style="display:flex;gap:8px;">
+<select id="watcherSheetSelect" style="flex:1;" onchange="onWatcherSheetSelected()"><option value="">Loading sheets…</option></select>
+<select id="watcherWorksheetSelect" style="flex:1;" onchange="onWatcherWorksheetSelected()" disabled><option value="">Select sheet first</option></select>
+</div>
+<div style="font-size:11px;color:var(--text3);margin-top:4px;">Not seeing your sheet? Connect Google Sheets under <strong>Sources</strong> first.</div>
+
+<div class="form-label">Trigger</div>
+<select id="watcherType" onchange="renderWatcherFields()">
+<option value="new_row">🆕 New Row — message the moment a row is added</option>
+<option value="date_reminder">🎂 Date Reminder — recurring wish/reminder (birthdays, fee due-dates, anniversaries…)</option>
+</select>
+
+<div style="display:flex;gap:8px;">
+<div style="flex:1;"><div class="form-label">Name column</div><select id="watcherNameCol"><option value="">— none —</option></select></div>
+<div style="flex:1;"><div class="form-label">Phone column</div><select id="watcherPhoneCol"><option value="">— none —</option></select></div>
+<div style="flex:1;"><div class="form-label">Email column</div><select id="watcherEmailCol"><option value="">— none —</option></select></div>
+</div>
+
+<div id="watcherDateFields" style="display:none;">
+<div style="display:flex;gap:8px;">
+<div style="flex:1;"><div class="form-label">Date column (birthday, due date, anniversary…)</div><select id="watcherDateCol"><option value="">— select column —</option></select></div>
+<div style="flex:1;"><div class="form-label">Send how many days before?</div><input type="number" id="watcherOffsetDays" value="0" min="0"><div style="font-size:11px;color:var(--text3);margin-top:2px;">0 = on the day itself, 1 = one day before, etc.</div></div>
+</div>
+</div>
+
+<div class="form-label">Send via</div>
+<select id="watcherChannel" onchange="renderWatcherFields()">
+<option value="whatsapp">📱 WhatsApp (approved template)</option>
+<option value="email">📧 Email</option>
+<option value="instagram">📷 Instagram</option>
+<option value="facebook">👥 Facebook</option>
+</select>
+
+<div id="watcherTemplateFields">
+<div class="form-label">Approved Template</div>
+<select id="watcherTemplateSelect" onchange="renderWatcherPlaceholderMapping()"><option value="">Loading templates…</option></select>
+<div style="font-size:11px;color:var(--text3);margin:4px 0 10px;">Only <strong>Meta-approved</strong> templates are shown — WhatsApp requires an approved template for messages like this that aren't a reply within an active chat. Manage templates under Campaigns → Templates.</div>
+<div id="watcherPlaceholderMapping"></div>
+</div>
+
+<div id="watcherPlainMessageField" style="display:none;">
+<div class="form-label">Message</div>
+<textarea id="watcherMessageTemplate" placeholder="Hi {name}, this is a reminder that…"></textarea>
+<div style="margin-top:6px;font-size:11px;color:var(--text3);">Insert field:</div>
+<div id="watcherFieldChips" style="margin-top:4px;"></div>
+</div>
+
+<div style="display:flex;gap:8px;">
+<div style="flex:1;"><div class="form-label">Check every</div><select id="watcherPollInterval"><option value="15">15 minutes</option><option value="30">30 minutes</option><option value="60">1 hour</option><option value="360">6 hours</option><option value="1440">Once a day</option></select></div>
+<div style="flex:1;display:flex;align-items:flex-end;padding-bottom:10px;"><label style="display:flex;align-items:center;gap:6px;font-size:13px;"><input type="checkbox" id="watcherActive" checked style="width:auto;"> Active</label></div>
+</div>
+
+<button class="btn btn-primary" style="margin-top:18px;width:100%;" onclick="saveWatcher()">💾 Save Watcher</button>
+</div>
+</div>
+
 <!-- ══ DASHBOARD AI ASSISTANT ══ -->
 <div class="assistant-fab" onclick="toggleAssistant()">🤖</div>
 <div class="assistant-panel" id="assistantPanel">
@@ -559,6 +630,7 @@ function loadTabData(tab) {
   if (tab === 'sources') loadIntegrations();
   if (tab === 'fieldmapping') { loadFieldMappings('webform'); loadFieldMappings('sheet'); }
   if (tab === 'automations') loadAutomations();
+  if (tab === 'sheetwatchers') loadSheetWatchers();
   if (tab === 'chatbot') loadChatbotConfig();
   if (tab === 'billing') loadBilling();
 }
@@ -1030,6 +1102,250 @@ async function deleteAutomation(id) {
   await apiCall(`/api/automations/${id}`, 'DELETE'); loadAutomations();
 }
 
+// ── SHEET WATCHERS (new-row auto-send + date reminders, approved templates) ──
+let watcherSheetsCache = [], watcherColumnsCache = [], watcherTemplatesCache = [], currentWatcherId = null;
+const WATCH_TYPE_LABEL = { new_row: '🆕 New Row', date_reminder: '🎂 Date Reminder' };
+
+async function loadSheetWatchers() {
+  const data = await apiCall('/api/sheet-watchers');
+  const el = document.getElementById('watchersList');
+  if (data.error || !data.watchers || !data.watchers.length) {
+    el.innerHTML = '<div class="empty-state"><div class="empty-icon">🔔</div><p>No sheet watchers yet. Connect a Google Sheet under Sources first, then add one here.</p></div>';
+    return;
+  }
+  el.innerHTML = data.watchers.map(w => {
+    const schedule = w.watch_type === 'date_reminder'
+      ? `${w.offset_days > 0 ? w.offset_days + 'd before' : 'on the day'} · every ${w.poll_interval_minutes}m`
+      : `every ${w.poll_interval_minutes}m`;
+    const status = w.last_error ? `<span class="badge badge-red" title="${escapeHtml(w.last_error)}">Error</span>`
+      : w.active ? '<span class="badge badge-green">Active</span>' : '<span class="badge badge-gray">Paused</span>';
+    return `<div class="data-row">
+      <div><div class="data-row-title">${WATCH_TYPE_LABEL[w.watch_type] || w.watch_type} — ${escapeHtml(w.spreadsheet_name || w.spreadsheet_id)} / ${escapeHtml(w.worksheet)}</div>
+      <div class="data-row-sub">${SOURCE_ICON[w.channel] || ''} ${w.channel} · ${schedule} · last checked ${timeAgo(w.last_polled_at)}</div></div>
+      <div class="data-row-actions">${status}
+        <button class="btn btn-ghost btn-xs" onclick="pollWatcherNow('${w.id}')">▶ Poll now</button>
+        <button class="btn btn-ghost btn-xs" onclick="editWatcher('${w.id}')">✏️</button>
+        <button class="btn btn-ghost btn-xs" onclick="deleteWatcher('${w.id}')">🗑</button>
+      </div></div>`;
+  }).join('');
+}
+
+async function openWatcherModal(id) {
+  currentWatcherId = id || null;
+  document.getElementById('watcherModalTitle').textContent = id ? 'Edit Sheet Watcher' : 'New Sheet Watcher';
+  document.getElementById('watcherType').value = 'new_row';
+  document.getElementById('watcherChannel').value = 'whatsapp';
+  document.getElementById('watcherOffsetDays').value = 0;
+  document.getElementById('watcherPollInterval').value = '15';
+  document.getElementById('watcherActive').checked = true;
+  document.getElementById('watcherMessageTemplate').value = '';
+  document.getElementById('watcherWorksheetSelect').innerHTML = '<option value="">Select sheet first</option>';
+  document.getElementById('watcherWorksheetSelect').disabled = true;
+  ['watcherNameCol','watcherPhoneCol','watcherEmailCol','watcherDateCol'].forEach(elId => document.getElementById(elId).innerHTML = '<option value="">— none —</option>');
+  renderWatcherFields();
+  openModal('watcherModal');
+
+  const [sheetsData, templatesData] = await Promise.all([
+    apiCall('/api/oauth/google/sheets'),
+    apiCall('/api/templates')
+  ]);
+  watcherSheetsCache = sheetsData.sheets || [];
+  const sheetSelect = document.getElementById('watcherSheetSelect');
+  sheetSelect.innerHTML = watcherSheetsCache.length
+    ? watcherSheetsCache.map(s => `<option value="${s.id}">${escapeHtml(s.name)}</option>`).join('')
+    : '<option value="">No sheets found — connect Google Sheets under Sources</option>';
+
+  watcherTemplatesCache = (templatesData.templates || []).filter(t => t.status === 'APPROVED');
+  const tplSelect = document.getElementById('watcherTemplateSelect');
+  tplSelect.innerHTML = watcherTemplatesCache.length
+    ? watcherTemplatesCache.map(t => `<option value="${t.id}">${escapeHtml(t.name)}</option>`).join('')
+    : '<option value="">No approved templates yet — submit one under Campaigns</option>';
+
+  if (id) {
+    const data = await apiCall('/api/sheet-watchers');
+    const w = (data.watchers || []).find(x => x.id === id);
+    if (!w) return;
+    document.getElementById('watcherType').value = w.watch_type;
+    document.getElementById('watcherChannel').value = w.channel;
+    document.getElementById('watcherOffsetDays').value = w.offset_days || 0;
+    document.getElementById('watcherPollInterval').value = String(w.poll_interval_minutes || 15);
+    document.getElementById('watcherActive').checked = !!w.active;
+    document.getElementById('watcherMessageTemplate').value = w.message_template || '';
+    renderWatcherFields();
+    if (watcherSheetsCache.some(s => s.id === w.spreadsheet_id)) {
+      sheetSelect.value = w.spreadsheet_id;
+      await onWatcherSheetSelected();
+      const wsSelect = document.getElementById('watcherWorksheetSelect');
+      if ([...wsSelect.options].some(o => o.value === w.worksheet)) {
+        wsSelect.value = w.worksheet;
+        await onWatcherWorksheetSelected();
+      }
+    }
+    ['watcherNameCol','watcherPhoneCol','watcherEmailCol','watcherDateCol'].forEach((elId, i) => {
+      const col = [w.name_column, w.phone_column, w.email_column, w.date_column][i];
+      if (col) document.getElementById(elId).value = col;
+    });
+    if (w.template_id) {
+      tplSelect.value = w.template_id;
+      renderWatcherPlaceholderMapping(w.placeholder_mapping || {});
+    }
+  }
+}
+
+function renderWatcherFields() {
+  const isDateReminder = document.getElementById('watcherType').value === 'date_reminder';
+  document.getElementById('watcherDateFields').style.display = isDateReminder ? 'block' : 'none';
+  const isWhatsapp = document.getElementById('watcherChannel').value === 'whatsapp';
+  document.getElementById('watcherTemplateFields').style.display = isWhatsapp ? 'block' : 'none';
+  document.getElementById('watcherPlainMessageField').style.display = isWhatsapp ? 'none' : 'block';
+  if (!isWhatsapp) renderWatcherFieldChips();
+}
+
+async function onWatcherSheetSelected() {
+  const sheetId = document.getElementById('watcherSheetSelect').value;
+  const wsSelect = document.getElementById('watcherWorksheetSelect');
+  if (!sheetId) { wsSelect.innerHTML = '<option value="">Select sheet first</option>'; wsSelect.disabled = true; return; }
+  wsSelect.innerHTML = '<option value="">Loading…</option>';
+  const data = await apiCall(`/api/oauth/google/worksheets/${sheetId}`);
+  wsSelect.disabled = false;
+  wsSelect.innerHTML = (data.worksheets || []).map(w => `<option value="${escapeHtml(w)}">${escapeHtml(w)}</option>`).join('') || '<option value="">No tabs found</option>';
+}
+
+async function onWatcherWorksheetSelected() {
+  const sheetId = document.getElementById('watcherSheetSelect').value;
+  const worksheet = document.getElementById('watcherWorksheetSelect').value;
+  if (!sheetId || !worksheet) return;
+  const data = await apiCall(`/api/oauth/google/columns/${sheetId}/${encodeURIComponent(worksheet)}`);
+  watcherColumnsCache = data.columns || [];
+  const options = '<option value="">— none —</option>' + watcherColumnsCache.map(c => `<option value="${escapeHtml(c)}">${escapeHtml(c)}</option>`).join('');
+  ['watcherNameCol','watcherPhoneCol','watcherEmailCol','watcherDateCol'].forEach(elId => {
+    const prev = document.getElementById(elId).value;
+    document.getElementById(elId).innerHTML = options;
+    if (prev) document.getElementById(elId).value = prev;
+  });
+  renderWatcherPlaceholderMapping();
+}
+
+// Same {{1}}/{{name}} extraction approach used for campaign templates in dashboard.html.
+function getWatcherTemplatePlaceholders(tpl) {
+  if (!tpl) return { keys: [], isPositional: true };
+  const body = tpl.body || '';
+  const rawKeys = [...new Set((body.match(/\{\{([^}]+)\}\}/g) || []).map(m => m.slice(2, -2).trim()))];
+  const isPositional = rawKeys.length > 0 && rawKeys.every(k => /^\d+$/.test(k));
+  const keys = isPositional ? rawKeys.map(k => parseInt(k, 10)).sort((a, b) => a - b) : rawKeys;
+  return { keys, isPositional };
+}
+
+function renderWatcherPlaceholderMapping(existingMapping) {
+  const tplId = document.getElementById('watcherTemplateSelect').value;
+  const tpl = watcherTemplatesCache.find(t => t.id === tplId);
+  const { keys } = getWatcherTemplatePlaceholders(tpl);
+  const container = document.getElementById('watcherPlaceholderMapping');
+  if (!tpl) { container.innerHTML = ''; return; }
+  if (!keys.length) { container.innerHTML = '<div style="font-size:11px;color:var(--text3);margin-bottom:10px;">This template has no variables to map.</div>'; return; }
+
+  const colOptions = watcherColumnsCache.map(c => `<option value="field:${escapeHtml(c)}">Sheet Column: ${escapeHtml(c)}</option>`).join('');
+  container.innerHTML = `<div class="form-label">Map "${escapeHtml(tpl.name)}" variables</div>` + keys.map(key => {
+    const saved = existingMapping?.[key];
+    const savedValue = saved ? (saved.type === 'field' ? `field:${saved.field}` : saved.type === 'custom' ? 'custom' : saved.type) : 'name';
+    return `<div style="display:flex;gap:8px;align-items:center;margin-bottom:6px;">
+      <span class="tag-chip">{{${key}}}</span>
+      <select id="watcherMap_${key}" style="flex:1;" onchange="toggleWatcherCustomInput('${key}')">
+        <option value="name">Name column</option>
+        <option value="phone">Phone column</option>
+        <option value="email">Email column</option>
+        ${colOptions}
+        <option value="custom">Fixed custom text</option>
+      </select>
+      <input type="text" id="watcherMapCustom_${key}" placeholder="Custom value" style="flex:1;display:${savedValue === 'custom' ? 'block' : 'none'};" value="${saved?.type === 'custom' ? escapeHtml(saved.value || '') : ''}">
+    </div>`;
+  }).join('');
+  keys.forEach(key => { const sel = document.getElementById(`watcherMap_${key}`); if (sel) sel.value = existingMapping?.[key] ? (existingMapping[key].type === 'field' ? `field:${existingMapping[key].field}` : existingMapping[key].type) : 'name'; });
+}
+function toggleWatcherCustomInput(key) {
+  const isCustom = document.getElementById(`watcherMap_${key}`).value === 'custom';
+  document.getElementById(`watcherMapCustom_${key}`).style.display = isCustom ? 'block' : 'none';
+}
+
+function renderWatcherFieldChips() {
+  const base = ['name','phone','email'].concat(watcherColumnsCache);
+  document.getElementById('watcherFieldChips').innerHTML = base.map(t =>
+    `<span class="tag-chip" style="cursor:pointer;" onclick="insertWatcherFieldTag('${t}')">+{${t}}</span>`).join(' ');
+}
+function insertWatcherFieldTag(tag) {
+  const ta = document.getElementById('watcherMessageTemplate');
+  const pos = ta.selectionStart || ta.value.length;
+  ta.value = ta.value.slice(0, pos) + `{${tag}}` + ta.value.slice(pos);
+  ta.focus();
+}
+
+async function saveWatcher() {
+  const sheetId = document.getElementById('watcherSheetSelect').value;
+  const worksheet = document.getElementById('watcherWorksheetSelect').value;
+  if (!sheetId || !worksheet) { showToast('Pick a sheet and tab first', true); return; }
+  const channel = document.getElementById('watcherChannel').value;
+  const watch_type = document.getElementById('watcherType').value;
+  const sheetName = watcherSheetsCache.find(s => s.id === sheetId)?.name || sheetId;
+
+  const body = {
+    spreadsheet_id: sheetId, spreadsheet_name: sheetName, worksheet, watch_type,
+    name_column: document.getElementById('watcherNameCol').value || null,
+    phone_column: document.getElementById('watcherPhoneCol').value || null,
+    email_column: document.getElementById('watcherEmailCol').value || null,
+    date_column: document.getElementById('watcherDateCol').value || null,
+    offset_days: parseInt(document.getElementById('watcherOffsetDays').value) || 0,
+    channel,
+    poll_interval_minutes: parseInt(document.getElementById('watcherPollInterval').value) || 15,
+    active: document.getElementById('watcherActive').checked
+  };
+
+  if (watch_type === 'date_reminder' && !body.date_column) { showToast('Pick a date column for date reminders', true); return; }
+  if (!body.phone_column && !body.email_column) { showToast('Map at least a phone or email column so there\'s somewhere to send to', true); return; }
+
+  if (channel === 'whatsapp') {
+    const templateId = document.getElementById('watcherTemplateSelect').value;
+    if (!templateId) { showToast('Pick an approved template', true); return; }
+    const tpl = watcherTemplatesCache.find(t => t.id === templateId);
+    const { keys } = getWatcherTemplatePlaceholders(tpl);
+    const placeholder_mapping = {};
+    for (const key of keys) {
+      const sel = document.getElementById(`watcherMap_${key}`);
+      if (!sel) continue;
+      const val = sel.value;
+      if (val === 'custom') {
+        const custom = document.getElementById(`watcherMapCustom_${key}`)?.value.trim() || '';
+        if (!custom) { showToast(`Custom value required for {{${key}}}`, true); return; }
+        placeholder_mapping[key] = { type: 'custom', value: custom };
+      } else if (val.startsWith('field:')) {
+        placeholder_mapping[key] = { type: 'field', field: val.slice(6) };
+      } else {
+        placeholder_mapping[key] = { type: val }; // 'name' | 'phone' | 'email'
+      }
+    }
+    body.template_id = templateId;
+    body.placeholder_mapping = placeholder_mapping;
+  } else {
+    body.message_template = document.getElementById('watcherMessageTemplate').value.trim();
+    if (!body.message_template) { showToast('Message is required', true); return; }
+  }
+
+  const endpoint = currentWatcherId ? `/api/sheet-watchers/${currentWatcherId}` : '/api/sheet-watchers';
+  const data = await apiCall(endpoint, currentWatcherId ? 'PUT' : 'POST', body);
+  if (data.error) { showToast('Failed: ' + data.error, true); return; }
+  showToast('✅ Watcher saved'); closeModal('watcherModal'); loadSheetWatchers();
+}
+
+async function editWatcher(id) { await openWatcherModal(id); }
+async function deleteWatcher(id) {
+  if (!confirm('Delete this watcher? It will stop sending immediately.')) return;
+  await apiCall(`/api/sheet-watchers/${id}`, 'DELETE'); loadSheetWatchers();
+}
+async function pollWatcherNow(id) {
+  const data = await apiCall(`/api/sheet-watchers/${id}/poll-now`, 'POST');
+  if (data.error) { showToast('Failed: ' + data.error, true); return; }
+  showToast('Polling within 60s…'); setTimeout(loadSheetWatchers, 65000);
+}
+
 // ── CHATBOT ──
 function addWidgetUrlField(val) {
   if (widgetUrls.length >= 5) { showToast('Max 5 URLs', true); return; }
@@ -1092,12 +1408,14 @@ async function subscribe(provider) {
 
 // ── INIT ──
 async function initApp() {
-  const profile = await apiCall('/api/profile');
-  if (profile && profile.email) {
+  const profileRes = await apiCall('/api/profile');
+  if (profileRes && profileRes.success && profileRes.user) {
+    const profile = profileRes.user;
     currentUser = profile;
-    document.getElementById('userName').textContent = profile.name || profile.email;
+    const displayName = profile.full_name || profile.email;
+    document.getElementById('userName').textContent = displayName;
     document.getElementById('userEmailDisplay').textContent = profile.email;
-    document.getElementById('avatarLetter').textContent = (profile.name || profile.email || 'U').charAt(0).toUpperCase();
+    document.getElementById('avatarLetter').textContent = (displayName || 'U').charAt(0).toUpperCase();
   }
   document.getElementById('app').style.display = 'block';
   loadLeads();
diff --git a/public/dashboard.html b/public/dashboard.html
index 7c3eb2c..8df5469 100644
--- a/public/dashboard.html
+++ b/public/dashboard.html
@@ -131,6 +131,7 @@ input:focus, select:focus, textarea:focus { outline: none; border-color: var(--g
 <div class="nav-item" data-tab="send"><span class="nav-icon">📤</span> Send</div>
 <div class="nav-item" data-tab="logs"><span class="nav-icon">📝</span> Campaign Logs</div>
 <div class="nav-item" data-tab="received"><span class="nav-icon">📥</span> Received <span id="receivedUnreadBadge" style="display:none;background:var(--red);color:#fff;border-radius:10px;font-size:10px;font-weight:700;padding:1px 6px;margin-left:6px;">0</span></div>
+<div class="nav-item" onclick="window.location.href='/crm.html'"><span class="nav-icon">🧩</span> CRM</div>
 </div>
 <div class="nav-section">
 <div class="nav-section-label">Account</div>
diff --git a/server.js b/server.js
index 0295cf6..6abe679 100644
--- a/server.js
+++ b/server.js
@@ -33,6 +33,9 @@ const meetingsRouter = require('./src/routes/meetings');
 const chatbotRouter = require('./src/routes/chatbot');
 const billingRouter = require('./src/routes/billing');
 const webhooksInboundRouter = require('./src/routes/webhooks-inbound');
+const sheetWatchersRouter = require('./src/routes/sheet-watchers');
+const { startSheetPoller } = require('./src/sheet-poller');
+const createChannelSender = require('./src/channel-send');
 
 const app = express();
 const PORT = process.env.PORT || 3000;
@@ -2264,11 +2267,26 @@ app.use('/api/meetings', meetingsRouter(crmDeps));
 app.use('/api', chatbotRouter(crmDeps)); // exposes /api/chatbot-config, /api/chatbot/*
 app.use('/api/billing', billingRouter(crmDeps));
 
+// /api/sheet-watchers — CRUD for polling-based sheet automations (Sheet
+// Reminders tab in the CRM). The actual polling loop is started separately
+// below via startSheetPoller; this was previously coded but never mounted,
+// so every request to it 404'd and the frontend tab always showed empty.
+app.use('/api/sheet-watchers', verifyUser, sheetWatchersRouter(crmDeps));
+
 // Public lead-capture endpoints (Google Sheet Apps Script, generic form tools)
 // and the authenticated "generate my webhook URL" endpoint that feeds them.
 app.use('/api/hooks', webhooksInboundRouter(crmDeps));
 app.use('/api/webhook-endpoints', webhooksInboundRouter.endpointsRouter(crmDeps));
 
+// Background poller for wb_sheet_watchers (new-row auto-sends + recurring
+// date reminders). Was previously written but never started — see
+// src/sheet-poller.js for the tick logic.
+startSheetPoller({
+  supabase,
+  fetch,
+  sendChannelMessage: createChannelSender(crmDeps)
+});
+
 // ================================================================
 // 16. API KEY MANAGEMENT ROUTES
 // (apiKeys module imported at top of file)
diff --git a/src/channel-send.js b/src/channel-send.js
index 07c59f7..a8d6686 100644
--- a/src/channel-send.js
+++ b/src/channel-send.js
@@ -1,8 +1,79 @@
 // src/channel-send.js — single place that knows how to actually send a message
-// on a given channel (WhatsApp today; Instagram/Facebook/Email queued until
-// their integrations are wired up). Used by both src/routes/leads.js (manual
-// replies) and src/routes/automations.js (auto first-touch/follow-up).
-module.exports = function createChannelSender({ supabase, decryptToken, META_API_VERSION, fetch }) {
+// on a given channel (WhatsApp, Instagram, Facebook, Email). Used by both
+// src/routes/leads.js (manual replies) and src/routes/automations.js /
+// src/sheet-poller.js (automated sends).
+//
+// Instagram/Facebook/Email previously just set status='queued' with a TODO —
+// they now use the tokens stored in wb_oauth_tokens (see src/google-auth.js
+// and src/routes/flows.js's OAuth callback), the same table the "Sources"
+// tab's Google/Facebook connect flow already populates, so no separate
+// token plumbing is needed here.
+module.exports = function createChannelSender({ supabase, decryptToken, encryptToken, META_API_VERSION, fetch }) {
+  const { getValidGoogleAccessToken } = require('./google-auth')({ supabase, encryptToken, decryptToken, fetch });
+
+  // Facebook/Instagram messaging both go through a Page (or the Page's linked
+  // IG business account) access token, obtained via the Facebook OAuth flow
+  // in flows.js and stored in wb_oauth_tokens.metadata.pages. This repo's
+  // OAuth flow only captures one connected Facebook identity per user, so —
+  // same simplifying assumption the rest of this CRM makes for WhatsApp
+  // ("most recent active wa_accounts row") — we use the first page returned.
+  async function getPageAccessToken(userId) {
+    const { data: row } = await supabase.from('wb_oauth_tokens')
+      .select('metadata').eq('user_id', userId).eq('service', 'facebook').single();
+    const page = row?.metadata?.pages?.[0];
+    if (!page?.access_token) throw new Error('Facebook/Instagram not connected — connect a Page under Sources in the CRM.');
+    return page;
+  }
+
+  async function sendFacebookMessage(userId, psid, text) {
+    const page = await getPageAccessToken(userId);
+    const res = await fetch(`https://graph.facebook.com/${META_API_VERSION}/me/messages?access_token=${encodeURIComponent(page.access_token)}`, {
+      method: 'POST',
+      headers: { 'Content-Type': 'application/json' },
+      body: JSON.stringify({ recipient: { id: psid }, message: { text } })
+    });
+    const data = await res.json();
+    if (!res.ok || !data.message_id) throw new Error(data.error?.message || `Facebook Graph API ${res.status}`);
+    return data.message_id;
+  }
+
+  // Instagram DMs use the same Graph "me/messages" call as Facebook Page
+  // messaging once a Page has a linked IG business account — the recipient
+  // id is the lead's Instagram-scoped id (IGSID), stored in wb_leads.ig_handle
+  // once a lead has been matched via the IG webhook.
+  async function sendInstagramMessage(userId, igsid, text) {
+    const page = await getPageAccessToken(userId);
+    const res = await fetch(`https://graph.facebook.com/${META_API_VERSION}/me/messages?access_token=${encodeURIComponent(page.access_token)}`, {
+      method: 'POST',
+      headers: { 'Content-Type': 'application/json' },
+      body: JSON.stringify({ recipient: { id: igsid }, message: { text } })
+    });
+    const data = await res.json();
+    if (!res.ok || !data.message_id) throw new Error(data.error?.message || `Instagram Graph API ${res.status}`);
+    return data.message_id;
+  }
+
+  // Sends via the Gmail API using the same Google OAuth token (with
+  // gmail.send scope) that the "Sources" Google connection already
+  // requests — refreshed on demand by getValidGoogleAccessToken, so this
+  // doesn't go stale after ~1hr the way the old wb_integrations-based
+  // Gmail token did.
+  async function sendEmail(userId, toEmail, subject, text) {
+    const accessToken = await getValidGoogleAccessToken(userId);
+    const raw = Buffer.from(
+      `To: ${toEmail}\r\nSubject: ${subject}\r\nContent-Type: text/plain; charset="UTF-8"\r\n\r\n${text}`
+    ).toString('base64').replace(/\+/g, '-').replace(/\//g, '_').replace(/=+$/, '');
+
+    const res = await fetch('https://gmail.googleapis.com/gmail/v1/users/me/messages/send', {
+      method: 'POST',
+      headers: { 'Content-Type': 'application/json', Authorization: `Bearer ${accessToken}` },
+      body: JSON.stringify({ raw })
+    });
+    const data = await res.json();
+    if (!res.ok || !data.id) throw new Error(data.error?.message || `Gmail API ${res.status}`);
+    return data.id;
+  }
+
   // `template`, when provided for the whatsapp channel, sends a Meta message
   // template ({ name, language, components }) instead of a free-text message.
   // WhatsApp requires this for any business-initiated send (reminders, wishes,
@@ -39,12 +110,27 @@ module.exports = function createChannelSender({ supabase, decryptToken, META_API
           externalId = responseData.messages[0].id;
         } catch (err) { sendError = err.message; }
       }
-    } else if (channel === 'instagram' || channel === 'facebook') {
-      // TODO: wire up once the Instagram/Facebook Meta app (Graph API messaging) is ready.
-      status = 'queued';
+    } else if (channel === 'facebook') {
+      if (!lead.fb_psid) {
+        sendError = 'Lead has no Facebook PSID on file (only inbound Messenger contacts can be replied to)';
+      } else {
+        try { externalId = await sendFacebookMessage(lead.user_id, lead.fb_psid, body); }
+        catch (err) { sendError = err.message; }
+      }
+    } else if (channel === 'instagram') {
+      if (!lead.ig_handle) {
+        sendError = 'Lead has no Instagram-scoped id on file (only inbound IG contacts can be replied to)';
+      } else {
+        try { externalId = await sendInstagramMessage(lead.user_id, lead.ig_handle, body); }
+        catch (err) { sendError = err.message; }
+      }
     } else if (channel === 'email') {
-      // TODO: wire up once Gmail send integration (wb_integrations type=gmail) is connected.
-      status = 'queued';
+      if (!lead.email) {
+        sendError = 'Lead has no email address on file';
+      } else {
+        try { externalId = await sendEmail(lead.user_id, lead.email, 'A message from your CRM', body); }
+        catch (err) { sendError = err.message; }
+      }
     } else {
       sendError = `Unsupported channel: ${channel}`;
     }